<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iappdragon.recruitmenttest">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstBugActivity" />
        <activity android:name=".SecondBugActivity" />
        <activity android:name=".ThirdBugActivity" />
        <activity android:name=".ForthBugActivity" />
        <activity android:name=".FifthBugActivity" />
        <activity android:name=".CameraActivity" />
    </application>

</manifest>